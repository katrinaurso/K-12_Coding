<h1>Schools#show</h1>

<% if @newsfeed %>
	<% @newsfeed.each do |news| %>
		<h2><%= news.title %></h2>
		<p><%= news.content %></p>
		<% @time = news.created_at.localtime %>
		<p><%= @time.strftime('%B %d, %Y - %I:%M %p') %></p>
	<% end %>
<% end %>